<br>
<h1>Categoria(s)</h1>
<br>

<div>
    <%= link_to 'Cadastrar nova Categoria', new_category_path %>
</div>
<br>
<div>
    <% if @categories.any? %>
        <table class="table">
            <thead>
                <tr>
                    <th>Codigo</th>
                    <th>Categoria</th>
                    <th>  </th>
                    <th>  </th>
                        
                </tr>
            </thead>
            <tbody class="table-group-divider">
                <% @categories.each do |c| %>
                    <tr>
                        <td align="center"><%= c.id %>   </td>
                        <td> <%= c.name %> </td>
                        <td> <%= link_to 'Editar', edit_category_path(c.id) %> </td>
                        <td id="<%= dom_id(c) %>" > <%= button_to 'Remover', category_path(c.id), method: :delete, class: 'btn btn-sm btn-danger'  %>  </td>
                        
                    </tr>
                <% end %>
            </tbody>
        </table>
    <% else %>
        <p> Não ha Categorias Cadastradas !!! </p>
    <% end %>
</div>
<br>
<div>
    <%= link_to 'Voltar', root_path %>
</div>